name: Check for new TimescaleDB release

on:
  schedule:
    - cron: "0 6 * * *"   # Daily at 06:00 UTC
  workflow_dispatch:

jobs:
  check:
    name: Check TimescaleDB release
    runs-on: ubuntu-latest

    permissions:
      contents: write
      actions: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get latest TimescaleDB release
        id: latest
        run: |
          tag=$(curl -fsSL \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/timescale/timescaledb/releases/latest \
            | jq -r '.tag_name')
          echo "tag=$tag" >> $GITHUB_OUTPUT
          echo "Latest TimescaleDB release: $tag"

      - name: Get last built version
        id: stored
        run: |
          if [ -f .timescaledb-version ]; then
            stored=$(cat .timescaledb-version)
          else
            stored=""
          fi
          echo "tag=$stored" >> $GITHUB_OUTPUT
          echo "Last built version: ${stored:-"(none)"}"

      - name: Trigger build and record version
        if: steps.latest.outputs.tag != steps.stored.outputs.tag && steps.latest.outputs.tag != ''
        run: |
          echo "New release detected (${{ steps.stored.outputs.tag }} → ${{ steps.latest.outputs.tag }}), triggering build"
          gh workflow run build.yml
          echo "${{ steps.latest.outputs.tag }}" > .timescaledb-version
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .timescaledb-version
          git commit -m "chore: bump tracked TimescaleDB version to ${{ steps.latest.outputs.tag }}"
          git push
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: No new release
        if: steps.latest.outputs.tag == steps.stored.outputs.tag
        run: echo "Already at ${{ steps.stored.outputs.tag }}, no build needed"
